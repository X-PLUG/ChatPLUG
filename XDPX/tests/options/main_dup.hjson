{
    __parent__: [
        ./default
        ./${config_dir}/batch
        ./${config_dir}/data
    ]
    __def__: {
        config_dir: config_group
        data_root: /path/to/root
    }
    amsgrad: true
    save_dir: output
}